// Allow up to 60 seconds for AI lesson generation in production
export const maxDuration = 60;

export default function NewLessonLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
